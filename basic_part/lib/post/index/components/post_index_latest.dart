import 'package:flutter/material.dart';

class PostIndexlastest extends StatelessWidget{
  @override
  Widget build (BuildContext context) {
    return Center(
      child: Icon(
        Icons.explore_outlined,
        size: 128,
        color: Colors.white
      ),
    );
  }
}